export default interface SnowOption {
    size? : number,
    maxSize? : number,
    speed? : number,
    style? : number,
    childSpan? : number,
    childQuantity? :number
    fallSpeed? : number,
    alphaSubtraction? : number
}
